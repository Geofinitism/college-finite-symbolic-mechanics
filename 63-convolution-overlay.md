# Essay Summary: Finite Overlap and Convolution

**File name:** `finite_overlap_and_convolution.md`  
**Corresponding PDF:** `papers/finite_overlap_and_convolution.pdf`  
**Date processed:** 2026-02-21  
**Status:** Published (Soft Geofinitism)

---

## Core trajectory

Makes the implicit sequential process in convolution explicit by defining a finite overlap operator 𝒪, then recovers classical forms as special cases.

## Key claim

Convolution, cross‑correlation, and autocorrelation are compressed traces of finite ordered procedures (displacement, interaction, accumulation). The finite overlap operator generalises them to bounded index sets and arbitrary interaction functions.

## Pillars

- **Pillar 1 (Geometric Container)** – Primary. The operator 𝒪 lives in a finite, high‑dimensional symbolic manifold; displacement δ and index set K define geometric relationships.
- **Pillar 5 (Finite Reality)** – Primary. No infinite sums: K ⊂ ℤ is explicitly finite. Infinity is treated as a process, not an attained limit.
- **Pillar 2 (Approximations)** – Secondary. Interaction function I may be non‑multiplicative, reflecting lossy transductions.
- **Pillar 3 (Dynamic Flow)** – Secondary. The sequence C(n) = 𝒪(f,g;n) is interpreted as a trajectory of interaction states.
- **Pillar 4 (Useful Fiction)** – Implicit. Classical convolution is retained as an idealised, useful special case.

## College

**College of Finite Symbolic Mechanics**  
(Technical core – FSM, FPU, overlap operators, delay embeddings, TBT.)

## Soft / Hard classification

**Soft Geofinitism** – Uses conventional mathematical notation, no Θ or tilde symbols. The afterword gestures toward the endogenous/exogenous framing and the generonic sphere without requiring those concepts.

## Key evidence / measurements

- Any actual computation of a convolution uses a finite index set K (finite support or truncation).
- The finite overlap operator recovers classical convolution when I(x,y)=x·y and the domain is treated as idealised.
- LLM resonance conjecture: measurable structural overlap without semantic reconstruction.

## Connected to

- **Takens‑Based Transformer (TBT)** – Delay embeddings in TBT are a direct application of the finite overlap logic.
- **Degradation experiments (JPEG on GPT‑2)** – Structured attractors appear under extreme compression, illustrating the “practically sufficient bound” notion.
- **Geofinitism seed document** – Pillars 1 and 5 are explicitly instantiated.
- **Conjecture of resonance** – Testable hypothesis for LLM interpretability.

## One resonant phrase

> “The symbolic form is not primary. It is the compressed trace of a finite ordered process.”

---

## Processing metadata (for factory use)

- **Time to process:** ~10 min (summary + cross‑refs)
- **Needs review?** No (already published)
- **Next action:** Add to College of Finite Symbolic Mechanics registry; optionally queue for Lesson 1 (Geometry Before Meaning)
- **Keywords:** convolution, finite overlap, non‑commutativity, 𝒪, Takens, embedding, LLM, resonance, Soft Geofinitism

---

## Path to PDF

The formatted PDF is stored at:  
`./papers/finite_overlap_and_convolution.pdf`  

(Adjust the path if your papers directory is elsewhere, e.g., `../papers/` or an absolute URL for web hosting.)